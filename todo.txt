fix EOFException on client shutdown
Put in checks to make sure client doesn't send more requests than it can handle
out of memory failures are visible
sort by map metadata (size)
analysis on map metadata
allow running on select maps
Add redundancy on last maps to account for slow computers

Interface:
FXN
	Start new run (autocomplete)
		view old run
		delete old run
	compare two runs
		filter by name
	view connections
  manage aliases
	
team A | team B | wins/wins | status | time (x)


? Error code to display incorrectly named runs
tooltip over aliased rows to display actual name
progress bar
login/register
